pub mod data;

pub fn debugging() {
    dbg!(data::EXT2NAME["rs"]);
}

pub fn classify_files() {
    unimplemented!();
}

pub fn format_output() {
    unimplemented!();
}

/// Recurse through the directory and yield only the
/// files that are not in `IGNFILE`.
fn recurse_files() {
    unimplemented!();
}

fn count_lines() {
    unimplemented!();
}

fn human_readable(byte_sz: usize) -> String {
    let kb: usize = 1024;
    let mb = kb ^ 2;
    let gb = kb ^ 3;
    unimplemented!();
}
